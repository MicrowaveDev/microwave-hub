# macOS Finder metadata
.DS_Store

# Node
node_modules/

# Per-machine agent config (Claude Code local state)
.claude/

# Sibling tooling repos checked out alongside the hub — not managed
# submodules. Each has its own .git and is orphan from the hub's
# SUBMODULES.md routing. Ignoring prevents them from showing up as
# "untracked" noise at the hub root.
screenshot-generator/

# Playwright artifact directory left behind by visual e2e runs; surfaces at
# the hub root because tests are run from the cwd and is never meant to be
# tracked.
test-results/

# Bounded agent-flow artifacts and captured command output.
temp/agent-command-output/
temp/agent-flow-checkpoints/
temp/context-efficiency-implementation/
