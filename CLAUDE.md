@AGENTS.md

## Claude Code

- Use `AGENTS.md` as the shared source of truth for hub workflow, routing, and safety rules.
- Keep Claude-specific notes here if they are ever needed, instead of forking the shared instructions.
